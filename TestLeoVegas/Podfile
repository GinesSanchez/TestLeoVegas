platform :ios, '13.0'

use_modular_headers!
use_frameworks!

target 'TestLeoVegas' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for TestLeoVegas
  pod 'Firebase/Analytics'
  pod 'Firebase/Crashlytics'
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'FirebaseFirestoreSwift'
  pod 'SwiftGen', '~> 6.0'

  target 'TestLeoVegasTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
